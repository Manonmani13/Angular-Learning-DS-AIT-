import { CityarraypipePipe } from './cityarraypipe.pipe';

describe('CityarraypipePipe', () => {
  it('create an instance', () => {
    const pipe = new CityarraypipePipe();
    expect(pipe).toBeTruthy();
  });
});
