import { ActconvertPipe } from './actconvert.pipe';

describe('ActconvertPipe', () => {
  it('create an instance', () => {
    const pipe = new ActconvertPipe();
    expect(pipe).toBeTruthy();
  });
});
